import 'package:flutter/material.dart';

class DialogSaveScore extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      width: 50,
      height: 50,
      decoration: BoxDecoration(
        color: Colors.red
      ),
      child: Text("a"),
    );
  }
}
